[tool.ruff]
target-version = "py311"
src = ["."]

[tool.black]
line-length = 88
target-version = ["py311"]

[tool.coverage.run]
source = ["app"]

[tool.coverage.report]
fail_under = 80
show_missing = true
exclude_lines = [
    "pragma: no cover",
    "if __name__ == .__main__.:",
]

